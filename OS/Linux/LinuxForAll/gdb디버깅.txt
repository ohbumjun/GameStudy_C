>> gcc -wall -g -o head head.c 
gcc 에 -g 옵션을 붙여야 한다.
이를 통해 gdb 를 이용하여 디버깅하겠다는 것을 알리는 것이다.

>> gdb ./head 
gdb 로 디버깅할 준비가 되었다는 것이다.

>> run -n 5
run 명령어를 통해 디버깅 대상 프로그램을 돌릴 수 잇다.
run 명령어에 옵션을 붙이면 해당 프로그램의 실행 인자가 된다.

>> backtrace
에러가 발생하면 일종의 호출 스택을 보여주는 함수이다.

>> frame 
